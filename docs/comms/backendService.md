The backend service is responsible for processing the data sent by the frontend and returning the results. It is implemented using a RESTful API and communicates with the frontend using HTTP requests and responses.

The backend service provides the following endpoints:
- `/candidate`: Handles the creation and retrieval of `candidate` entities.
