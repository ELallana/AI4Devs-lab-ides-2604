---
id: P2
name: User cancel the candidateAdd page
---
## Scope
Frontend.

## Prerequisites
The user is in the `candidateAdd` page.

## Initiator
The user clicks the button `cancel`.

## Steps
1. The `candidateAdd` page is closed, all its data is discarded, and the `main` page is loaded.